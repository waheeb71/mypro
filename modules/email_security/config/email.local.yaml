# ═══════════════════════════════════════════════════════════════════
#  Enterprise CyberNexus — Email Security Local Overrides
#  File: config/email.local.yaml
#
#  🚫 لا تُرفع لـ git — مُستبعد تلقائياً من .gitignore
#  يُستخدم لوضع مفاتيح API والإعدادات المحلية الخاصة
# ═══════════════════════════════════════════════════════════════════

email_security:
  # مثال: تغيير الوضع لاختبار بدون حجب
  # mode: monitor

  # وضع إعدادات DNS (إذا كنت تستخدم خادماً داخلياً)
  # يمكنك إضافة نطاقاتك الموثوقة هنا
  whitelist:
    domains:
      # - "yourcompany.com"
      # - "partner-company.net"
